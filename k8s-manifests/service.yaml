apiVersion: v1
kind: Service
metadata:
  name: xebikart-state-api
  labels:
    app: xebikart
    tier: state-api
spec:
  type: NodePort
  ports:
    - port: 8080
  selector:
    app: xebikart
    tier: state-api
